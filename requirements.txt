# Core dependencies
asyncio-atexit==1.0.1
mcp>=1.0.0

# OSINT Tools - These will be installed automatically
sherlock-project
holehe

# Production Features
starlette>=0.27.0
uvicorn>=0.24.0

# Optional tool with heavier native dependency chain on some Python versions:
# maigret

# Optional tool that may require custom install/source package:
# theHarvester

# Note: SpiderFoot, GHunt, and Blackbird require manual installation.
